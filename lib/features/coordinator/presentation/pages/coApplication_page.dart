import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class CoordinatorApplicationsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Coordinator Applications'),
      ),
      body: Center(
        child: Text('This is the Coordinator Applications page.'),
      ),
    );

  }
}