import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class ResourceResponsesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('ResourceResponsesPage'),
      ),
      body: Center(
        child: Text('This is the ResourceResponsesPage.'),
      ),
    );

  }
}